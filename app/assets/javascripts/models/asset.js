function Asset(object) {
  this.id = object.id
  this.caption = object.caption
  this.direct_upload_url = object.direct_upload_url
  this.experience_id = object.experience_id
  // this.link = object.link
  // this.content_type = object.content_type
  // this.upload_file_name = object.upload_file_name
  // this.upload_file_size = object.upload_file_size
  this.user_id = object.user_id
}

