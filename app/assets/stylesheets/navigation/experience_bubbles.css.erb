.experience-bubble {
  width: 90px;
  height: 90px;
  border-radius: 100%;
  margin: 0 auto;
}

.round-button-circle {
  box-shadow: 0 0 5px white;
  transition: box-shadow .2s ease;
  opacity: 5;
}

nav {
  width: 120px;
  height: 100vh;
  overflow: auto;
  position: fixed;
  margin: 10px;
  background-color: transparent;
  overflow-x: hidden;
  overflow-y: auto;
  z-index: 1;
}

::-webkit-scrollbar {
    display: none;
}
